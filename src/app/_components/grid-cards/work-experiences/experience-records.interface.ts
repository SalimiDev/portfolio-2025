export interface ExperienceRecordTypes {
    title: string; // Job title
    company: string; // Company name
    dates: string; // Duration of the role
    opacity: number; // Opacity level for display
    lineThrough: boolean; // Indicates if the title should have a strikethrough
}
